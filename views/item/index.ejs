<%- layout('layout') -%>

  <section class="section">
    <div class="container">
      <div class="columns">
        <div class="column is-one-third">
          <% include ../partials/category_menu %>
        </div>
        <div class="column is-two-thirds dfc-item-list">

          <% if (search.length > 0) { %>
            <h2 class="subtitle">
              Here are your search results for "<%= search %>"</h2>
            <% } %>
          <% items.forEach(item => { %>
            <% include ../components/item_card %>
          <% }) %>
        </div>
      </div>
    </div>
  </section>